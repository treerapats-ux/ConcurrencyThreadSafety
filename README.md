# LAB : Concurrency & Thread Safety

**Software Construction 3(2-2-5)** · ปฏิบัติการ 2 ชั่วโมง
ใช้คู่กับคาบบรรยายเรื่อง Race Condition, Mutual Exclusion และ Message Passing

---

## สิ่งที่จะได้จากแลปนี้

เมื่อทำครบทุกด่าน นิสิตจะสามารถ

1. **พิสูจน์** ได้ว่าโปรแกรมเดียวกันให้ผลไม่เหมือนกันในแต่ละครั้งที่รัน
2. **ชี้** ได้ว่า critical section เริ่มและจบที่บรรทัดไหน พร้อมบอกเหตุผล
3. **แก้** race condition ด้วยการล็อกที่ครบและถูกจุด
4. **อธิบาย** ได้ว่าทำไม deadlock จึงเกิด และแก้ด้วย lock ordering
5. **เปรียบเทียบ** วิธีล็อกกับวิธี message passing ได้

> ข้อ 2 คือหัวใจของแลปนี้ และมีน้ำหนักคะแนนมากที่สุด
> การใส่ `synchronized` ให้ครบทุกเมธอดโดยไม่เข้าใจว่าทำไม จะได้คะแนนไม่เต็ม

---

## ไฟล์ในโฟลเดอร์นี้

| ไฟล์ | ต้องแก้ไหม | หน้าที่ |
|---|---|---|
| `Account.java` | **ใช่** | บัญชีธนาคาร มี `TODO 1.1` `1.2` `1.3` |
| `Bank.java` | **ใช่** | การโอนเงิน มี `TODO 2` |
| `RaceDemo.java` | ไม่ | ทดลอง `deposit` — lost update |
| `OverdraftDemo.java` | ไม่ | ทดลอง `withdraw` — ถอนเกินยอด |
| `DeadlockDemo.java` | ไม่ | ทดลอง `transfer` — ค้าง และความเป็นหน่วยเดียว |
| `ActorDemo.java` | ไม่ | เวอร์ชันที่ไม่ใช้ล็อกเลย ไว้ดูตอนท้าย |

**แลปนี้ไม่มีไฟล์เทสต์แยกต่างหาก** เดโมแต่ละตัวบอกผลตัดสินในตัวเองอยู่แล้ว
ว่ารอบไหนผิด ผิดไปเท่าไร และให้ไปแก้ `TODO` ข้อไหน

### วิธีคอมไพล์และรัน

> **ถ้าเครื่องมีคอร์เดียว** (เช่น VM หรือ container บางตัว) เดโมอาจขึ้นว่าถูกต้อง
> ทั้งที่โค้ดยังผิด ให้รันซ้ำ
> เหตุผลอยู่ในหัวข้อ "ทำไมแลปนี้ถึงตรวจงานแปลกกว่าแลปอื่น" ท้ายเอกสาร

---

## ส่วนที่ 1 · เห็นมันพังก่อน

**ยังไม่ต้องแก้โค้ดใด ๆ ทั้งสิ้น**

`Account.deposit()` มีบรรทัดเดียวคือ `balance = balance + amount;`
เธรด 4 ตัว ฝากเงินตัวละ 50,000 ครั้ง ครั้งละ 1 บาท ยอดสุดท้ายต้องได้ 200,000 เสมอ

```bash
รัน RaceDemo
```

### สิ่งที่ต้องทำ

1. รันแล้วคัดลอกตาราง 20 รอบเก็บไว้
2. รันซ้ำอีกครั้ง แล้วสังเกตว่าตารางรอบใหม่**ไม่เหมือนเดิม**
3. บันทึกลงรายงาน: มีกี่รอบที่ผิด และหายมากที่สุดกี่บาท

รอบที่ 1
 * === Demo 1: Lost Update (deposit) ===
4 threads, each depositing 1 unit 50000 times
The correct total is 200000 in every round

 round  |   actual total  | expected |   lost
--------+-----------------+----------+---------
   1    |        65,253   |  200,000  |  134,747   <-- WRONG
   2    |        99,742   |  200,000  |  100,258   <-- WRONG
   3    |       111,411   |  200,000  |  88,589   <-- WRONG
   4    |       151,198   |  200,000  |  48,802   <-- WRONG
   5    |       200,000   |  200,000  |       0
   6    |       200,000   |  200,000  |       0
   7    |       200,000   |  200,000  |       0
   8    |       200,000   |  200,000  |       0
   9    |       200,000   |  200,000  |       0
  10    |       200,000   |  200,000  |       0
  11    |       200,000   |  200,000  |       0
  12    |       200,000   |  200,000  |       0
  13    |       200,000   |  200,000  |       0
  14    |       200,000   |  200,000  |       0
  15    |       200,000   |  200,000  |       0
  16    |       100,000   |  200,000  |  100,000   <-- WRONG
  17    |       200,000   |  200,000  |       0
  18    |       200,000   |  200,000  |       0
  19    |       200,000   |  200,000  |       0
  20    |       200,000   |  200,000  |       0

=== Experiment summary ===
Rounds with a wrong total : 5 / 20 ผิด 5
Largest amount lost       : 134747 หายมากสุด

รอบที่ 2

=== Demo 1: Lost Update (deposit) ===
4 threads, each depositing 1 unit 50000 times
The correct total is 200000 in every round

 round  |   actual total  | expected |   lost
--------+-----------------+----------+---------
   1    |        56,882   |  200,000  |  143,118   <-- WRONG
   2    |        85,020   |  200,000  |  114,980   <-- WRONG
   3    |       129,807   |  200,000  |  70,193   <-- WRONG
   4    |        73,591   |  200,000  |  126,409   <-- WRONG
   5    |       200,000   |  200,000  |       0
   6    |       200,000   |  200,000  |       0
   7    |       200,000   |  200,000  |       0
   8    |       200,000   |  200,000  |       0
   9    |       200,000   |  200,000  |       0
  10    |       200,000   |  200,000  |       0
  11    |       200,000   |  200,000  |       0
  12    |       200,000   |  200,000  |       0
  13    |       200,000   |  200,000  |       0
  14    |       200,000   |  200,000  |       0
  15    |       200,000   |  200,000  |       0
  16    |       200,000   |  200,000  |       0
  17    |       200,000   |  200,000  |       0
  18    |       200,000   |  200,000  |       0
  19    |       200,000   |  200,000  |       0
  20    |       200,000   |  200,000  |       0

=== Experiment summary ===
Rounds with a wrong total : 4 / 20 ผิด 4
Largest amount lost       : 143118 หายมากสุด


### คำถามที่ต้องตอบในรายงาน

> **Q1** โค้ดไม่ได้เปลี่ยนเลยสักตัวอักษร แล้วทำไมผลถึงต่างกันทุกครั้งที่รัน

answer: เพราะ thread หลายอัน ทำงานพร้อมกันซึ่งลำดับการทำงานของ thread กำหนดโดย thread Scheduler ซึ่งอาจสลับลำดับในแต่ละครั้ง ทำให้เกิด Race Condition และ Lost Update

> **Q2** ถ้าเจอบั๊กแบบนี้ในระบบจริง ทำไมการ "ใส่ `System.out.println` ไล่ดู" ถึงมักทำให้บั๊กหายไปเฉย ๆ

answer: เพราะการเพิ่ม การบวนการปริ้นท์มาอาจทำให้ลำดับการทำงานของ thread เปลี่ยนไป bug จึงหายไปในบางครั้ง เพราะthread ทำงานไม่พร้อมกัน
---

## ส่วนที่ 2 · ชี้ให้ได้ก่อนแก้ 

### 2.1 ระบุ critical section ก่อน — ยังไม่ต้องเขียนโค้ด

เปิด `Account.java` แล้วตอบสามข้อนี้กับ**ทุกเมธอด** ในคลาส

| คำถาม | ถ้าตอบ "ไม่" |
|---|---|
| บรรทัดนี้แตะ field ที่ไม่ใช่ตัวแปร local หรือไม่ | จบ ไม่ต้องคุ้มครอง |
| มีเธรดอื่นแตะตัวเดียวกันได้หรือไม่ | จบ ไม่ต้องคุ้มครอง |
| ถ้าถูกแทรกกลางคัน ผลจะผิดหรือไม่ | จบ ไม่ต้องคุ้มครอง |

**ตอบ "ใช่" ครบสามข้อ = ต้องคุ้มครอง**

เขียนลงรายงานเป็นตาราง

| เมธอด | critical section เริ่ม–จบบรรทัดที่ | เหตุผล |
|---|---|---|
| `deposit` | บรรทัดที่ 55  | เป็นการอ่านและเขียนค่า balance ซึ่งเป็น shared field หากหลายเธรดทำพร้อมกันอาจเกิด Lost Update ทำให้ยอดเงินสุดท้ายผิด |

| `withdraw` | บรรทัดที่ 71-73 | ต้องตรวจสอบ balance และแก้ไข balance เป็นการทำงานเดียวกัน หากมีเธรดอื่นแทรกกลางระหว่างการตรวจสอบกับการหักเงิน อาจทำให้เกิดการถอนเกินยอดหรือยอดเงินผิด |

| `balance` | บรรทัดที่ 42 | แม้จะเป็นการอ่านอย่างเดียว แต่ balance เป็น shared field ที่เธรดอื่นสามารถแก้ไขได้ จึงต้องคุ้มครองเพื่อให้การอ่านและการเขียนระหว่างเธรดมีความสอดคล้องกัน
 |
| `id` | บรรทัดที่ 80 | เป็นอ่านอย่างเดียวและค่าไม่เปลี่ยน |

> **จุดที่คนพลาดมากที่สุด** `id()` กับ `balance()` ต่างก็เป็นการอ่านอย่างเดียวทั้งคู่
> แต่ควรได้คำตอบไม่เหมือนกัน อธิบายให้ได้ว่าเพราะอะไร

answer: เพราะ ถึงแม้ balance จะเป็นอ่านอย่างเดียวแต่ balance เป็นค่าที่เปลี่ยนแปลงได้จาก thread อื่น จึงไม่เหมือนกับ id ที่ค่าไม่เปลี่ยน

### 2.2 แก้ TODO 1.1, 1.2, 1.3 

ทีนี้ค่อยลงมือแก้ `Account.java` แล้วตรวจด้วยเดโมสองตัว

```bash
รัน RaceDemo        # ต้องได้ 0/20 รอบที่ผิด
รัน OverdraftDemo   # ต้องได้ 0/20 รอบที่ผิด
```

`OverdraftDemo` ทดสอบสิ่งที่ `RaceDemo` มองไม่เห็น
มันเอาเงิน 100,000 บาทมาตั้ง แล้วปล่อย 4 เธรดพยายามถอนทีละบาทรวม 200,000 ครั้ง
ถ้า `withdraw` ถูกต้อง ต้องถอนสำเร็จพอดี 100,000 ครั้งแล้วเหลือ 0 พอดี
ถ้ายังผิด จะได้เห็น**ยอดติดลบ**กับตาตัวเอง ทั้งที่ในโค้ดมี `if` ป้องกันอยู่ชัด ๆ

> แก้แค่ `TODO 1.1` แล้ว `RaceDemo` จะเขียวทันที แต่ `OverdraftDemo` จะยังแดงอยู่
> อย่าเพิ่งไปส่วนถัดไปจนกว่าจะเขียวทั้งคู่

บันทึกตารางใหม่ของทั้งสองเดโมลงรายงาน คู่กับตารางก่อนแก้

### คำถามที่ต้องตอบ

> **Q3** `withdraw` อันตรายกว่า `deposit` อย่างไร ทั้งที่ทั้งคู่ก็แค่บวกลบเลขตัวเดียว

answer: เพราะ withdraw ต้องตรวจสอบ balance ก่อนว่ามีเงินเพียงพอหรือไม่ แล้วจึงหักเงินออก หากมีหลายเธรดทำงานพร้อมกัน อาจมีเธรดอื่นเปลี่ยนค่า balance ระหว่างการตรวจสอบกับการหักเงิน ทำให้เกิดการถอนเกินยอดหรือทำให้ยอดเงินผิดได้ ต่างจาก deposit ที่เป็นการเพิ่มเงินอย่างเดียว

> **Q4** ถ้าใส่ `synchronized` ให้ `deposit` กับ `withdraw` แต่**ไม่ใส่ให้ `balance()`** จะเกิดอะไรขึ้น และทำไมเทสต์อาจจับไม่ได้

answer: ถ้าไม่ใส่ synchronized thread อาจจะทำให้ balance ที่ ไม่มี synchronized แสดงค่าที่ผิดพลาดในบางครั้งซึ่ง โอกาศอาจเกิดได้น้อยเทสจึงจับไม่ได้ แต่ยังคงมี race condition
---

## ส่วน 3 · ยาแก้ก็มีผลข้างเคียง

ตอนนี้ `Account` ปลอดภัยแล้ว แต่ `Bank.transfer` ยังมีปัญหาของมันเอง

```bash
รัน DeadlockDemo
```

โปรแกรมจะ**ค้าง** โดยไม่มี error ไม่มี exception ไม่มีอะไรแดงสักบรรทัด
เดโมใช้ `ThreadMXBean` ของ JVM ยืนยันให้ว่าเป็น deadlock จริง พร้อมบอกชื่อเธรดที่ล็อกกันอยู่

เดโมนี้ทำงานสองเฟส

| เฟส | ตรวจอะไร | รันเมื่อไร |
|---|---|---|
| **A** | โปรแกรมค้างหรือไม่ | ทุกครั้ง |
| **B** | การโอนยังเป็นหน่วยเดียวหรือไม่ | เฉพาะเมื่อเฟส A ผ่าน |

### สิ่งที่ต้องทำ

1. บันทึกผลเฟส A ตอนที่ยังค้าง ลงรายงาน
2. อ่าน `TODO 2` ใน `Bank.java` แล้วแก้
3. รันใหม่ ต้องผ่านทั้งเฟส A และเฟส B

phase A:
--- Phase A: does it hang? ---
Waiting at most 8 seconds
Elapsed: 55 ms
Result : STUCK (DEADLOCK)
The JVM confirms 2 deadlocked thread(s):
   - mover-A-to-B
   - mover-B-to-A

Thread-1 holds A and waits for B.
Thread-2 holds B and waits for A.
Neither will ever let go.

Note there is no error, no exception, nothing in red.
The program simply stopped.

Fix TODO 2 in Bank.java and run this file again.
(Phase B is skipped while the program still hangs.)

> **ทางลัดที่ห้ามใช้** อย่าแก้ deadlock ด้วยการถอด `synchronized` ใบในออก
>
> ทำแบบนั้น deadlock หายจริง และถ้าวัดยอดรวมตอนงานจบหมดแล้วก็จะได้ 2,000,000 พอดี
> เพราะทุกการถอนมีการฝากคู่กันเสมอ บัญชีจึงกลับมาบาลานซ์ตอนจบ
>
> แต่เฟส B มีเธรดผู้ตรวจสอบคอยอ่านยอดรวม **ระหว่างที่เงินกำลังเคลื่อน**
> ช่วงที่หักจากต้นทางไปแล้วแต่ยังไม่ทันเพิ่มให้ปลายทาง เงินก้อนนั้นไม่ได้อยู่ที่ไหนเลย
> ผู้ตรวจสอบจะเห็นยอดรวมขาดหายทันที และเดโมจะฟ้องพร้อมบอกว่าขาดไปเท่าไร

### คำถามที่ต้องตอบ

> **Q5** อธิบายว่าทำไมการเรียงลำดับล็อกตาม `id` ถึงทำให้ deadlock เกิดไม่ได้เลย ไม่ใช่แค่เกิดยากขึ้น

answer: เพราะการเรียงลำดับการล็อกตาม id ทำให้ทุกเธรดต้องล็อกบัญชีที่มี id น้อยกว่าก่อนเสมอ จึงไม่สามารถเกิดการรอกันเป็นวงกลมได้ เช่น ถ้าเธรดหนึ่งถือบัญชี id = 1 และรอ id = 2 อีกเธรดก็จะต้องล็อก id = 1 ก่อนเช่นกัน ไม่สามารถถือ id = 2 แล้วไปรอ id = 1 ได้ ดังนั้นจึงไม่มีเงื่อนไขของ deadlock และ deadlock จึงเกิดไม่ได้ ไม่ใช่แค่เกิดยากขึ้น

> **Q6** ถ้าเปลี่ยนไปใช้ล็อกใบเดียวคุมทั้งธนาคาร (`synchronized (Bank.class)`) จะแก้ deadlock ได้ไหม และต้องแลกกับอะไร

answer: ได้ เพราะการใช้ synchronized (Bank.class) เป็นล็อกเดียวสำหรับทั้งธนาคาร จะทำให้มีเพียงหนึ่งเธรดที่สามารถทำ transfer ได้ในแต่ละครั้ง จึงไม่สามารถเกิด deadlock จากการล็อกบัญชีหลายใบพร้อมกันได้ แต่ต้องแลกกับประสิทธิภาพและความสามารถในการทำงานพร้อมกัน เพราะการโอนเงินทุกครั้งต้องรอคิวเดียวกัน ทำให้ concurrency และ scalability ลดลง

> **Q7** สมมติมีคนแก้ deadlock ด้วยการถอดล็อกใบในออก แล้ววัดยอดรวมหลังงานจบก็ได้ 2,000,000 พอดีทุกครั้ง
> เขาสรุปว่าโค้ดถูกต้องแล้ว เขาผิดตรงไหน และต้องวัดอย่างไรถึงจะเห็นปัญหา

answer:  เขาผิดเพราะตรวจสอบเฉพาะยอดรวม หลังจากการโอนทั้งหมดเสร็จแล้ว
ซึ่งตอนนั้นเงินที่ถอนออกจากบัญชีต้นทางได้ถูกฝากเข้าบัญชีปลายทางเรียบร้อยแล้ว ทำให้ยอดรวมกลับมาเป็น 2,000,000 แม้ระหว่างการโอนจะมีช่วงที่เงินหายไปชั่วคราวก็ตาม

วิธีตรวจที่ถูกต้องคือต้องมีเธรดอื่นคอยตรวจสอบ balance ของทั้งสองบัญชี ระหว่างที่กำลังโอนเงิน และต้องอ่านโดยถือ lock ของทั้งสองบัญชีไว้ เพื่อดูว่าทุกครั้งยอดรวมยังคงเป็น 2,000,000 หรือไม่ หากพบยอดรวมน้อยกว่า 2,000,000 แสดงว่า transfer ไม่ได้เป็น atomic

---

## ส่วนที่ 4 · ถ้าไม่แชร์กันตั้งแต่แรกล่ะ

```bash
รัน ActorDemo
```

ไฟล์นี้ทำงานเดียวกับ `RaceDemo` ทุกประการ แต่**ไม่มีคำว่า `synchronized` อยู่เลยสักตัว**
และให้ผลถูกต้องทุกครั้ง เพราะ `balance` ไม่ได้ถูกแชร์ มันมีเจ้าของอยู่เธรดเดียว
เธรดอื่นทำได้อย่างเดียวคือฝากข้อความไว้ในคิว

อ่านโค้ดให้จบ แล้วตอบ

> **Q8** วิธีนี้กำจัด race condition ได้โดยไม่ต้องล็อกเลย แล้วทำไมเราไม่ใช้วิธีนี้กับทุกอย่างในโลก
> ยกข้อเสียมาอย่างน้อยสองข้อ

answer: 1. โค้ดซับซ้อนขึ้น 2.มี overhead และทำงานช้าลง 
3.ไม่ได้เหมาะกับทุกกรณีโดยเฉพาะงานที่ต้องการให้หลายเธรดเข้าถึงข้อมูลร่วมกันหรือการตอบสนองที่รวดเร็ว   



### เรื่องที่ต้องเข้าใจให้ตรงกัน

เทสต์ที่เคยเขียนมาเป็น **deterministic** คือ input เดิมได้ output เดิมเสมอ
แต่ race condition ไม่ใช่แบบนั้น มันขึ้นกับจังหวะที่ scheduler สลับเธรด
ซึ่งต่างกันทุกครั้งที่รัน และต่างกันในแต่ละเครื่อง

เดโมทุกตัวจึงใช้วิธีเดียวกันคือ **ทำซ้ำหลายรอบภายใต้แรงกดดันสูง**
และปล่อยเธรดพร้อมกันด้วย `CountDownLatch` เพราะถ้าปล่อยไล่ทีละตัว
เธรดแรกอาจทำงานจบก่อนที่เธรดสุดท้ายจะได้เริ่ม แล้วจะไม่มีการทับซ้อนกันเลย

ผลที่ตามมา

> **เดโมขึ้น 0/20 แปลว่าโค้ดน่าจะถูก ไม่ได้แปลว่าโค้ดถูกแน่นอน**

นี่คือธรรมชาติของการทดสอบโปรแกรมหลายเธรด ไม่ใช่ข้อบกพร่องของเดโม
และเป็นเหตุผลว่าทำไมความปลอดภัยของเธรดจึงต้องมาจาก**การออกแบบ**
ไม่ใช่จากการไล่จับทีหลัง

`DeadlockDemo` เป็นข้อยกเว้นเดียว เพราะเฟส A ใช้ `ThreadMXBean` ให้ JVM
ยืนยันเองว่ามี deadlock จริง ไม่ได้เดาจากเวลาที่ใช้ จึงเชื่อถือได้เต็มที่

## ติดตรงไหน ลองดูตรงนี้ก่อนถาม


**เดโมค้างไม่ยอมจบ** — `DeadlockDemo` มี timeout 8 วินาทีอยู่แล้ว ถ้าเดโมตัวอื่นค้าง ให้กด `Ctrl+C` แล้วตรวจว่าเผลอใส่ล็อกซ้อนกันใน `Account` หรือไม่


**`RaceDemo` เขียวแล้วแต่ `OverdraftDemo` ยังแดง** — ปกติ แปลว่าแก้ `TODO 1.1` แล้วแต่ยังไม่ได้แก้ `TODO 1.2` ซึ่งเป็นคนละปัญหากัน

**`RaceDemo` ไม่เคยผิดเลยสักรอบตั้งแต่แรก** — เครื่องเร็วเกินหรือคอร์น้อยเกิน ลองเพิ่ม `THREADS` หรือ `OPS_PER_THREAD` ในไฟล์ 

**เฟส A ผ่านแล้วแต่เฟส B แดง** — แก้ deadlock ถูกทางแล้ว แต่การโอนไม่เป็นหน่วยเดียวอีกต่อไป มักเกิดจากการถอดล็อกใบในออก ให้ใส่กลับแล้วแก้ที่ *ลำดับ* การขอล็อกแทน
